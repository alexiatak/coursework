# coursework
use this script for every object of the data files. Copy the script in the object's folder(where there should be another seperate folder for the fits files of the object) and run it in the mark environment(conda activate mark),which lays on the script folder.

Make any necessary changes to the script depending on the star's brightness(parameter LEVELS=99 or =80 if it is bright),the position of the star's reflection(parameter MANUAL_FOR_ALL_SHOTS = True, if the reflection is not symmetrical).
